import { powerDigitSum } from '../proj-0016/index'

describe('Solution for Power Digit Sum', () => {
  const answer = powerDigitSum()
  it('Should have the correct result', () => {
    expect(answer).toBe(1366)
  })
})
