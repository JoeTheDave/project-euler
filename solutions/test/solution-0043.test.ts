import { subStringDivisibility } from '../proj-0043/index'

describe('Solution for Sub String Divisibility', () => {
  const answer = subStringDivisibility()
  it('Should have the correct result', () => {
    expect(answer).toBe(16695334890)
  })
})
