import { champernownesConstant } from '../proj-0040/index'

describe('Solution for Champernownes Constant', () => {
  const answer = champernownesConstant()
  it('Should have the correct result', () => {
    expect(answer).toBe(210)
  })
})
