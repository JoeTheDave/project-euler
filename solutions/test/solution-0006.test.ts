import { sumSquareDifference } from '../proj-0006/index';

describe('Solution for Sum Square Difference', () => {
  const answer = sumSquareDifference();
  it('Should have the correct result', () => {
    expect(answer).toBe(25164150);
  });
});
