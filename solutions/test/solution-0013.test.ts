import { largeSum } from '../proj-0013/index'

describe('Solution for Large Sum', () => {
  const answer = largeSum()
  it('Should have the correct result', () => {
    expect(answer).toBe('5537376230')
  })
})
